# Release notes

One file per released version, named `X.Y.Z.md`, written for users (highlights, install
snippets, migration notes). The release workflow refuses to run without the file for the requested
version and uses it as the GitHub Release body. `CHANGELOG.md` at the repository root stays the
exhaustive, grouped record; this directory carries the narrative.
